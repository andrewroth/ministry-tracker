<%= f.text_field input_stat[:column], :size => 4, :class => "positiveinteger", :value => new_record ? stat_data.count : nil %>
<%= link_to_remote '<div class="magnifier_icon">&nbsp;</div>', :url => {:controller => :monthly_reports, :action => :stat_details, :stats => stat_data, :stat_meta_data => input_stat}, :before => "$('#spinner#{input_stat[:column]}').show();", :success => "show_dialog('Manage #{input_stat[:label]}'); $('#spinner#{input_stat[:column]}').hide();", :html => { :title => 'Show details' } %>
&nbsp;<%= spinner(input_stat[:column]) %>