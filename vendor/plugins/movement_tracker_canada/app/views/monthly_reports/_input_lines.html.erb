    <% form_for(@monthly_report) do |f| %>
      <%= f.error_messages %>
      <%= f.hidden_field :month_id %>
      <%= f.hidden_field :campus_id %>
      <table>
<%
stats_reports[:monthly_report].each do | input_stat |
%>
        <tr class="statsFormField">
          <td><%= f.label input_stat[:column], input_stat[:label] %></td>
          <td><%= f.text_field input_stat[:column], :size => 4 %></td>
        </tr>

<%
end
%>      </table>
      <p>
        <br/>
        <div class="center"><%= f.submit "Submit" %></div>
      </p>
      <br/>
    <% end %>
