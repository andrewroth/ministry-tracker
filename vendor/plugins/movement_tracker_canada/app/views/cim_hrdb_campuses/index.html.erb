<h1>Listing Campuses</h1>
<br/>
<table>
  <tr>
    <th>Description</th>
    <th>Region</th>
    <th>Province</th>
  </tr>

<% @campuses.each do |campus| %>
  <tr>
    <td><%=h campus.campus_desc %></td>
    <td><%=h campus.region.desc %></td>
    <td>
    <% if campus.state_id %>
      <%=h campus.state.name %>
    <% end %>
    </td>
    <td><%= link_to 'Show', cim_hrdb_campus_path(campus) %></td>
    <td><%= link_to 'Edit', edit_cim_hrdb_campus_path(campus) %></td>
    <td><%= link_to 'Destroy', cim_hrdb_campus_path(campus), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New campus', new_cim_hrdb_campus_path %>