<h1>Editing Person</h1>
<br/>
<h2><%=h @cim_hrdb_person.id.to_s + " - " + @cim_hrdb_person.first_name + " " + @cim_hrdb_person.last_name %></h2>
<br/>

<%= "<p>" + @cim_hrdb_person.first_name + " has never been granted staff access, to do so now check the box below:</p>" if @cim_hrdb_person.cim_hrdb_staff.nil? %>
<p>
  <%= check_box_tag :is_active, "", @is_active_staff %>
  <%= observe_field :is_active, :url => {:action => 'change_or_add', :controller => "cim_hrdb_staff", :person_id => @cim_hrdb_person.id}, :before => "$('#spinneria').show()", :complete => "$('#spinneria').hide()" %>
  <b>Is active staff</b>
  <%= spinner('ia') %>
</p>

<br/>
<p>
  <b>Year in School:</b>
  <%= render(:partial => "person_year" , :object => @person_years) %>
</p>
<br/>

<br/>
<p>
  <b>Assignments:</b>
  <%= render(:partial => "assignments" , :object => @assignments) %>
</p>
<br/>

<br/>
<%= link_to 'Back', cim_hrdb_people_path %>
