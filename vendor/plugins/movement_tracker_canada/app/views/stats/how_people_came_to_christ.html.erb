<a name="top"><h1 id="statsTitle">Measurements</h1></a>
<div class="backLink"><a href="/stats">Go back</a></div>

<br/>

<div class="statsFormContainer">
  <div class="statsFormBox">
    <h2 class="statsFormBoxTitle">How people came to Christ</h2>
    <br/>

    <div class="statsForm">
      <% form_for :report, :url => { :action => :how_people_came_to_christ }, :html => { :name => 'reportForm', :onSubmit => "$('#spinnersubmit').show()" } do |form| %>

        <p class="statsFormField">
          <%= form.label :ministry, "Ministry:" %>
          <%= form.collection_select :ministry, @ministries, :id, :name, :selected => @stats_ministry_id.to_i %>
        </p>

        <p class="statsFormField">
          <%= form.label :year, "Year:" %>
          <%= form.select :year, @years, :selected => @year_selected %>
        </p>

        <p>
          <%= submit_tag "Show report" , :class => "submit" %>
        </p>
        <%= spinner('submit') %>

      <% end %>
    </div>
  </div>
</div>






<br/>
<div id="results" class="statsContainer" <% unless @stats_ministry_selected.present? %> style="display: none"<% end %> >
  <% if @stats_ministry_selected.present? %>
    <p>
      <span class="statsTitle"><%= sprintf("%s", @stats_ministry_selected.name) %> during <%= sprintf("%s", @year_selected) %></span>
    </p>
    <p>
      <%= render :partial => "how_people_came_to_christ" %>
      <br/>
    </p>
  <% end %>
</div>
