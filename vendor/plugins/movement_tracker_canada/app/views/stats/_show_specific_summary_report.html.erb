<%
      if permission_granted 
        stat_report(report_symbol).each do | key, input_stat |
          %><%= show_stat_hash_line(@period_model, @campus_ids, input_stat)%><%
        end
      end 
%>