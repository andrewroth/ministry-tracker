<p class="statsTitle"><%= @report_description %></p>
<table class="statsTable">
    <tr>
        <td>
        </td>

        <% @periods.each do |p| %>
          <td class="statsTableTitle">
            <%= period_description_for_column_title(p) %>
          </td>
        <% end %>

        <td class="statsTableTitle">
            Total
        </td>

    </tr>
    <% @reports_to_show.each do |r| %>
      <%= show_summary_report(r, true)%>
    <% end %>
</table>
