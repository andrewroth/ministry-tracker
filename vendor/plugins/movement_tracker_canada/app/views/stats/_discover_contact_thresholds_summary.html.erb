<p><span class="statsTitle">
  Discover &#8211; Thresholds Summary View at <%= @ministry_name %>
</span></p>

<br/>

<table class="dataTable">
  <thead>
    <tr>
      <th>Threshold</th>
      <th>Threshold Order</th>
      <th>Number of Contacts</th>
    </tr>
  </thead>
  <tbody>
    <% total_contacts = 0 %>
    <% @contacts_grouped_by_next_step_id.each do |next_step_id, contacts| %>
      <tr>
        <td><%= Contact::NEXT_STEP_OPTIONS[next_step_id][0] %></td>
        <td><%= Contact::NEXT_STEP_OPTIONS[next_step_id][1] %></td>
        <td><%= contacts.size %></td>
        <% total_contacts += contacts.size.to_i %>
      </tr>
    <% end %>
  </tbody>
  <tfoot>
    <tr class="statsTableTotal">
      <td>Total</td>
      <td></td>
      <td><%= total_contacts %></td>
    </tr>
  </tfoot>
</table>

<script>
  $(function() {
    extendDataTableOptions = {
      "aaSorting": [[0, "asc"]],
      "aoColumns": [
        { "aDataSort": [ 1 ] },
        { "bVisible": false },
        null
      ]
    }
  })
</script>