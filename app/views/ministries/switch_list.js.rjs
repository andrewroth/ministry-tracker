page['switch_ministries_list'].show();
page << "$('#switch_ministries_list').css('left', $('#title').offset().left);"
page << "$('#switch_ministries_list').css('top', $('#title').offset().top + $('#title').height());"
page['switch_ministries_list'].replace_html render(:partial => 'ministries/choose_ministry')
