page['switch_ministries_list'].replace_html render(:partial => 'ministries/choose_ministry')
#page << "$('#switch_ministries_list').css('left', $('#switch_ministries').position().left - $('#switch_ministries_list').width() + $('#switch_ministries').width() );"
page << "$('#switch_ministries_list').css('top', $('#switch_ministries').position().top + $('#switch_ministries').height());"
page['switch_ministries_list'].slideDown();
