page << "$('#switch_ministries_list').css('left', $('#switch_ministries').offset().left);"
page << "$('#switch_ministries_list').css('top', $('#switch_ministries').offset().top + $('#switch_ministries').height());"
page['switch_ministries_list'].replace_html render(:partial => 'ministries/choose_ministry')
page['switch_ministries_list'].slideDown();