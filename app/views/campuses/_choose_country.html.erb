Pick a country:
<%=
  countries2 = [['-- Choose Country --','']] + @countries
  select_tag("country", options_for_select(countries2, @country))
%>

<%=
  observe_field(:country,
    { :url => change_country_campuses_url,
      :with => 'country',
      :loading => "$('#spinner1').show()"})
%>

<%= spinner(1) %>
