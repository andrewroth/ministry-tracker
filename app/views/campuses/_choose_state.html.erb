Pick a state:
<%=
  states2 = ["-- Choose #{I18n.t :State, :default => 'State'} --", ''] + @states
  select_tag("state", options_for_select(states2))
%>

<%=
  observe_field(:state,
    { :url => change_state_campuses_path,
      :with => %|'state='+$('#state')[0].value+'&country=#{params[:country]}'|,
      :loading => "$('#spinner2').show()"})
%>	

<%= spinner(2) %>
