Pick a state:
<%=
  ss = @states.collect {|s| [s.name, s.id]}
  ss = ss.insert(0, ["-- Choose #{I18n.t :State, :default => 'State'} --", ''])
  
  select_tag("state", options_for_select(ss))
%>

<%=
  observe_field(:state,
    { :url => change_state_campuses_path,
      :with => 'state_id',
      :loading => "$('#spinner2').show()"})
%>	

<%= spinner(2) %>