<h1>New column</h1>

<%= error_messages_for :column %>

<% form_for(:column, :url => columns_path) do |f| %>
<%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', columns_path %>