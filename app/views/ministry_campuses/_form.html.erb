<%- unless @campuses -%>
To find the campus you'd like to add, first choose a country, then a state (if applicable), then the high school or college you'd like to add. <br /><br />
<%- end -%>

<%- form_remote_tag :url => ministry_campuses_path do -%>
  <%= hidden_field_tag :ministry_id, @ministry.id %>
	<div id="countries"><%= render :partial => 'campuses/choose_country' if @countries %></div>
	<div id="states"><%= render :partial => 'campuses/choose_state' if @states %></div>
	<div id="colleges"><%= render :partial => 'campuses/choose_college' if @colleges %></div>
	<%= submit_tag "Add Campus to Ministry", :id => 'add_college', :style => 'display:none' %>
<%- end -%>
