<%= select_tag :primary_campus_id, options_for_select(Array.wrap(@campuses.collect {|c| [c.name, c.id]}).insert(0, ['','']), @person.primary_campus.try(:id)), :style => 'width:300px' %>

<%= link_to_function(t(:person_campus_change_state, :default => 'change to a different state'), "$('#campuses').hide();$('#states').show();") %>