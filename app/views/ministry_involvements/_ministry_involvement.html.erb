<%= link_to_remote image_tag('delete.png'), :url => person_ministry_involvement_url(@person.id, ministry_involvement.id, :from_manage => true), :method => :delete %> 
<%= ministry_involvement.ministry.name %>, since <%= ministry_involvement.start_date %>
<%= link_to_remote image_tag('button_edit_tiny.gif'), :url => edit_person_ministry_involvement_url(@person.id, ministry_involvement.id), :method => :get %> 
