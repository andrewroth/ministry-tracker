
<div style="width: 70%; margin-left: auto; margin-right: auto;">
<% if @involvements %>

  <br/>
  <big><%= image_tag("silk/error.png") %> You're about to change all of the following people's roles to the role that you select below, regardless of their current role.</big>
  <br/>
  <br/>
  A person may have different roles at different ministries, only the role at the ministry specified for each person below will be changed.
  <br/>
  <br/>
  <br/>

  <table cellspacing="0" cellpadding="5" border="0" class="groupInvolvementList">
    <tr>
      <th>Person</th>
      <th>Ministry</th>
      <th>Current Role</th>
    </tr>
    <% @involvements.each do |involvement| %>
    <tr class="<%= cycle('highlight', 'nohighlight') %>">
      <td><%= link_to("#{involvement.person.full_name}", "/people/#{involvement.person.id}", {:title => "Goto #{involvement.person.first_name.capitalize}'s profile"}) -%></td>
      <td><%= involvement.ministry.name %></td>
      <td><%= involvement.ministry_role.name %> (<%= involvement.ministry_role.class.to_s.underscore.humanize %>)</td>
    </tr>
    <% end %>
  </table>

  <br/>
  <br/>
  
  <% form_tag({:action => "update_multiple_roles", :controller => "ministry_involvements", :method => "get"},
    {:onSubmit => "if( $('#role_id').val() == '' || $('#role_id :selected').text() == '') { alert('Please select a new role to change to first!'); return false; } else { return true; }"}) do %>
    
    <big>
    <%= image_tag("silk/pencil.png") %>
    Change to the new role
    <%= grouped_collection_select('role', :id, [ StaffRole, StudentRole ], :all, :human_name_plural, :id, :name, :include_blank => true) %>
    </big>
  
    <% for involvement in @involvements %>
      <%= hidden_field_tag "involvement_id[]", involvement.id %>
    <% end %>

    <br/>
    <br/>
    <%= submit_tag "Change roles" %> or <%= link_to("Cancel", :back) %>
    <br/>
    
  <% end %>

<% else %>
  <%= link_to("Back", :back) %>
<% end %>
</div>